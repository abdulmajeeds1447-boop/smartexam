import { ExamEnvelope, Student, Teacher } from './types';

// Empty arrays as requested to start fresh
export const MOCK_STUDENTS: Student[] = [];

export const MOCK_TEACHERS: Teacher[] = [];

export const INITIAL_EXAMS: ExamEnvelope[] = [];